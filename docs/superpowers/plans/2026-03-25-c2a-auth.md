# C2-A — Autenticação Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar autenticação por email+senha ao KanbanLicita com proteção automática de rotas, tela de login e script de criação do primeiro admin.

**Architecture:** NextAuth.js v5 (beta) com Credentials provider e JWT strategy. Quatro modelos Prisma novos (User, Account, Session, VerificationToken) com nomes em inglês exigidos pelo `@auth/prisma-adapter` e tabelas mapeadas para português. Proteção de rotas via `proxy.ts` (Next.js 16 — `middleware.ts` foi depreciado).

**Tech Stack:** next-auth@beta, @auth/prisma-adapter, bcryptjs, Prisma 7 (driver adapter mode), Next.js 16.2.1 App Router

---

## Spec de referência

`docs/superpowers/specs/2026-03-25-c2a-auth-design.md`

## Importante: leia antes de escrever qualquer código

- `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md` — proxy.ts (Next.js 16, não mais middleware.ts)
- `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md` — Route handlers

---

## Mapa de arquivos

| Arquivo | Ação |
|---|---|
| `prisma/schema.prisma` | Modificar — adicionar 4 modelos |
| `auth.ts` | Criar — config central do NextAuth |
| `proxy.ts` | Criar — proteção de rotas |
| `app/api/auth/[...nextauth]/route.ts` | Criar — handler HTTP do NextAuth |
| `app/login/page.tsx` | Criar — Server Component da tela de login |
| `app/login/LoginForm.tsx` | Criar — Client Component do formulário |
| `types/next-auth.d.ts` | Criar — extensão de tipos da sessão |
| `scripts/seed-admin.ts` | Criar — script do primeiro admin |
| `app/page.tsx` | Modificar — verificar sessão antes de redirecionar |
| `.env` | Modificar — adicionar AUTH_SECRET |
| `.env.example` | Modificar — documentar AUTH_SECRET |
| `__tests__/auth/authorize.test.ts` | Criar — testes da lógica de autorização |

---

## Task 1: Dependências e variáveis de ambiente

**Files:**
- Modify: `package.json` (via npm install)
- Modify: `.env`
- Modify: `.env.example` (se existir)

- [ ] **Step 1: Instalar dependências**

```bash
cd "C:/Users/vinic/OneDrive/PROJETOS/APPS/kanbamlicita"
npm install next-auth@beta @auth/prisma-adapter bcryptjs
npm install -D @types/bcryptjs
```

Esperado: instalação sem erros. Verificar que `next-auth`, `@auth/prisma-adapter` e `bcryptjs` aparecem em `package.json`.

- [ ] **Step 2: Adicionar AUTH_SECRET ao .env**

Gerar string segura:
```bash
openssl rand -base64 32
```

Adicionar ao `.env` (linha após DATABASE_URL):
```env
AUTH_SECRET=<resultado_do_comando_acima>
```

- [ ] **Step 3: Documentar AUTH_SECRET no .env.example**

Se o arquivo `.env.example` existir, adicionar:
```env
AUTH_SECRET=gere_com_openssl_rand_-base64_32
```

Se não existir, criar `.env.example` com:
```env
DATABASE_URL=postgresql://user:password@localhost:5432/kanbamlicita
AUTH_SECRET=gere_com_openssl_rand_-base64_32
```

- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json .env.example
git commit -m "feat(c2a): install next-auth, prisma-adapter, bcryptjs"
```

(Nunca commitar `.env` — contém segredos.)

---

## Task 2: Schema Prisma — 4 modelos de autenticação

**Files:**
- Modify: `prisma/schema.prisma` (final do arquivo, após o último model)

- [ ] **Step 1: Adicionar modelos ao schema**

Abrir `prisma/schema.prisma` e adicionar ao final do arquivo (após `LicitacaoAliasOrigem`):

```prisma
// ─── Autenticação (NextAuth v5) ───────────────────────────────────────────────
// IMPORTANTE: Os nomes de modelo em inglês são exigidos pelo @auth/prisma-adapter.
// As tabelas no banco são mapeadas para português via @@map.

model User {
  id            String    @id @default(uuid())
  email         String    @unique @db.VarChar(255)
  emailVerified DateTime? @map("email_verified")
  senha         String?   @db.VarChar(255)
  name          String?   @db.VarChar(100)
  image         String?
  role          String    @default("user") @db.VarChar(20)
  ativo         Boolean   @default(true)
  criadoEm      DateTime  @default(now()) @map("criado_em")
  atualizadoEm  DateTime  @updatedAt @map("atualizado_em")

  accounts Account[]
  sessions Session[]

  @@map("usuarios")
}

// Usado apenas com OAuth (C2-B). Com Credentials provider, não é populado.
model Account {
  id                String  @id @default(uuid())
  userId            String  @map("user_id")
  type              String
  provider          String
  providerAccountId String  @map("provider_account_id")
  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@map("contas_auth")
}

// Não utilizado com strategy: 'jwt'. Existe para satisfazer a interface do adapter.
model Session {
  id           String   @id @default(uuid())
  sessionToken String   @unique @map("session_token")
  userId       String   @map("user_id")
  expires      DateTime

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("sessoes_auth")
}

// Não utilizado com strategy: 'jwt'. Existe para satisfazer a interface do adapter.
model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
  @@map("verificacao_tokens")
}
```

- [ ] **Step 2: Rodar a migração**

```bash
npx prisma migrate dev --name add-auth-models
```

Esperado: migração aplicada com sucesso, 4 novas tabelas criadas: `usuarios`, `contas_auth`, `sessoes_auth`, `verificacao_tokens`.

- [ ] **Step 3: Verificar que o cliente Prisma foi regenerado**

```bash
npx prisma generate
```

Esperado: sem erros. O cliente em `lib/generated/prisma` agora inclui `db.user`, `db.account`, `db.session`, `db.verificationToken`.

- [ ] **Step 4: Commit**

```bash
git add prisma/schema.prisma prisma/migrations/
git commit -m "feat(c2a): add User, Account, Session, VerificationToken models"
```

---

## Task 3: Lógica de autorização + testes

**Files:**
- Create: `lib/auth/authorize.ts`
- Create: `__tests__/auth/authorize.test.ts`

A lógica de `authorize` é extraída para uma função pura e testável, depois importada em `auth.ts`.

- [ ] **Step 1: Escrever o teste antes da implementação**

Criar `__tests__/auth/authorize.test.ts`:

```ts
import { buildAuthorize } from '@/lib/auth/authorize'
import bcrypt from 'bcryptjs'

describe('buildAuthorize', () => {
  const HASH_CORRETA = bcrypt.hashSync('senha123', 10)

  const usuarioAtivo = {
    id: 'u1',
    email: 'user@test.com',
    name: 'Usuário',
    role: 'user',
    ativo: true,
    senha: HASH_CORRETA,
  }

  function makeFindUser(user: typeof usuarioAtivo | null) {
    return async (_email: string) => user
  }

  it('retorna usuário com credenciais corretas e ativo=true', async () => {
    const authorize = buildAuthorize(makeFindUser(usuarioAtivo))
    const result = await authorize('user@test.com', 'senha123')
    expect(result).toEqual({
      id: 'u1',
      email: 'user@test.com',
      name: 'Usuário',
      role: 'user',
    })
  })

  it('retorna null com senha errada', async () => {
    const authorize = buildAuthorize(makeFindUser(usuarioAtivo))
    const result = await authorize('user@test.com', 'senha_errada')
    expect(result).toBeNull()
  })

  it('retorna null quando usuário não existe', async () => {
    const authorize = buildAuthorize(makeFindUser(null))
    const result = await authorize('inexistente@test.com', 'senha123')
    expect(result).toBeNull()
  })

  it('retorna null quando ativo=false mesmo com senha correta', async () => {
    const inativo = { ...usuarioAtivo, ativo: false }
    const authorize = buildAuthorize(makeFindUser(inativo))
    const result = await authorize('user@test.com', 'senha123')
    expect(result).toBeNull()
  })

  it('normaliza email para lowercase', async () => {
    let emailRecebido = ''
    const findUser = async (email: string) => {
      emailRecebido = email
      return usuarioAtivo
    }
    const authorize = buildAuthorize(findUser)
    await authorize('User@TEST.com', 'senha123')
    expect(emailRecebido).toBe('user@test.com')
  })
})
```

- [ ] **Step 2: Rodar o teste e confirmar que falha**

```bash
NODE_OPTIONS='--experimental-vm-modules' npx jest __tests__/auth/authorize.test.ts --no-coverage
```

Esperado: FAIL — "Cannot find module '@/lib/auth/authorize'"

- [ ] **Step 3: Criar `lib/auth/authorize.ts`**

```ts
// lib/auth/authorize.ts
import bcrypt from 'bcryptjs'

type UserRow = {
  id: string
  email: string
  name: string | null
  role: string
  ativo: boolean
  senha: string | null
}

type AuthResult = {
  id: string
  email: string
  name: string | null
  role: string
} | null

export function buildAuthorize(
  findUser: (email: string) => Promise<UserRow | null>
) {
  return async function authorize(
    rawEmail: string | undefined,
    senha: string | undefined
  ): Promise<AuthResult> {
    const email = rawEmail?.toLowerCase().trim()
    if (!email || !senha) return null

    const user = await findUser(email)
    if (!user?.senha || !user.ativo) return null

    const ok = await bcrypt.compare(senha, user.senha)
    if (!ok) return null

    return { id: user.id, email: user.email, name: user.name, role: user.role }
  }
}
```

- [ ] **Step 4: Rodar o teste e confirmar que passa**

```bash
NODE_OPTIONS='--experimental-vm-modules' npx jest __tests__/auth/authorize.test.ts --no-coverage
```

Esperado: PASS — 5/5 testes passando.

- [ ] **Step 5: Commit**

```bash
git add lib/auth/authorize.ts __tests__/auth/authorize.test.ts
git commit -m "feat(c2a): add authorize logic with tests"
```

---

## Task 4: `auth.ts` — Configuração central do NextAuth

**Files:**
- Create: `auth.ts` (raiz do projeto)

- [ ] **Step 1: Criar `auth.ts`**

**Nota:** Este `auth.ts` usa `buildAuthorize` de `lib/auth/authorize.ts` (criado na Task 3) em vez de ter a lógica de `bcrypt` inline. Isso diverge intencionalmente do snippet na spec — a factory `buildAuthorize` foi extraída para permitir testes unitários sem depender do banco.

```ts
// auth.ts
import NextAuth from 'next-auth'
import { PrismaAdapter } from '@auth/prisma-adapter'
import Credentials from 'next-auth/providers/credentials'
import { db } from '@/lib/db'
import { buildAuthorize } from '@/lib/auth/authorize'

const authorize = buildAuthorize((email) =>
  db.user.findUnique({ where: { email } })
)

export const { auth, handlers, signIn, signOut } = NextAuth({
  adapter: PrismaAdapter(db),
  session: { strategy: 'jwt' },
  providers: [
    Credentials({
      credentials: {
        email: { label: 'Email', type: 'email' },
        senha: { label: 'Senha', type: 'password' },
      },
      async authorize(credentials) {
        return authorize(
          credentials?.email as string,
          credentials?.senha as string
        )
      },
    }),
  ],
  callbacks: {
    jwt({ token, user }) {
      if (user) {
        token.id = user.id
        token.role = (user as { role: string }).role
      }
      return token
    },
    session({ session, token }) {
      if (session.user) {
        session.user.id = token.id as string
        session.user.role = token.role as string
      }
      return session
    },
  },
  pages: {
    signIn: '/login',
  },
})
```

- [ ] **Step 2: Verificar compilação TypeScript**

```bash
npx tsc --noEmit
```

Esperado: sem erros relacionados a `auth.ts`. (Pode haver erros sobre `session.user.id` — resolvidos na Task 6 com `types/next-auth.d.ts`.)

- [ ] **Step 3: Commit**

```bash
git add auth.ts
git commit -m "feat(c2a): add auth.ts NextAuth configuration"
```

---

## Task 5: `proxy.ts` + handler da API + tipos da sessão

**Files:**
- Create: `proxy.ts` (raiz do projeto)
- Create: `app/api/auth/[...nextauth]/route.ts`
- Create: `types/next-auth.d.ts`

**Leia antes:** `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md`

- [ ] **Step 1: Criar `proxy.ts`**

No Next.js 16, `middleware.ts` foi renomeado para `proxy.ts` e o export deve se chamar `proxy`. O NextAuth exporta `auth as middleware` que não pode ser re-exportado diretamente.

**Abordagem canônica do NextAuth v5:** usar `auth` como wrapper com callback. Com JWT strategy, o NextAuth lê o cookie e redireciona para `/login` automaticamente quando não há sessão (conforme `pages.signIn` configurado em `auth.ts`). O redirect de `/login` → `/kanban` para usuário já logado é feito manualmente dentro do callback.

```ts
// proxy.ts
import { auth } from '@/auth'
import { NextResponse } from 'next/server'

export const proxy = auth((request) => {
  // Se chegou aqui, request.auth contém a sessão (usuário autenticado)
  // Redirecionar de /login para /kanban se já logado
  if (request.nextUrl.pathname === '/login') {
    return NextResponse.redirect(new URL('/kanban', request.url))
  }
  // Todas as outras rotas: deixar passar (auth() redireciona para /login se não autenticado)
})

export const config = {
  matcher: [
    '/((?!api/auth|_next/static|_next/image|favicon\\.ico|sitemap\\.xml|robots\\.txt).*)',
  ],
}
```

**Nota:** `request` aqui é do tipo `NextAuthRequest` (extends `NextRequest`) com campo `request.auth` contendo a sessão. Se o TypeScript reclamar do tipo, usar `(request as any).auth` ou verificar o tipo exportado pelo `next-auth@beta` instalado.

Se a abordagem com `auth(callback)` não funcionar com a versão beta instalada (erro de tipo ou runtime), alternativa direta:

```ts
// proxy.ts — alternativa se auth(callback) não funcionar
import { auth } from '@/auth'
import { NextRequest, NextResponse } from 'next/server'

export async function proxy(request: NextRequest) {
  const session = await auth()
  const isLoginPage = request.nextUrl.pathname === '/login'
  if (!session && !isLoginPage) return NextResponse.redirect(new URL('/login', request.url))
  if (session && isLoginPage) return NextResponse.redirect(new URL('/kanban', request.url))
}

export const config = {
  matcher: ['/((?!api/auth|_next/static|_next/image|favicon\\.ico|sitemap\\.xml|robots\\.txt).*)'],
}
```

- [ ] **Step 2: Criar `app/api/auth/[...nextauth]/route.ts`**

```ts
// app/api/auth/[...nextauth]/route.ts
import { handlers } from '@/auth'
export const { GET, POST } = handlers
```

- [ ] **Step 3: Criar `types/next-auth.d.ts`**

O diretório `types/` já existe no projeto.

```ts
// types/next-auth.d.ts
import { DefaultSession } from 'next-auth'

declare module 'next-auth' {
  interface Session {
    user: {
      id: string
      role: string
    } & DefaultSession['user']
  }
}
```

- [ ] **Step 4: Verificar compilação TypeScript**

```bash
npx tsc --noEmit
```

Esperado: sem erros. Os campos `session.user.id` e `session.user.role` devem ser reconhecidos pelo TypeScript.

- [ ] **Step 5: Commit**

```bash
git add proxy.ts app/api/auth/ types/next-auth.d.ts
git commit -m "feat(c2a): add proxy, auth route handler, session types"
```

---

## Task 6: Tela de login

**Files:**
- Create: `app/login/page.tsx`
- Create: `app/login/LoginForm.tsx`

- [ ] **Step 1: Criar `app/login/LoginForm.tsx` (Client Component)**

```tsx
// app/login/LoginForm.tsx
'use client'

import { useState } from 'react'
import { signIn } from 'next-auth/react'

export function LoginForm() {
  const [email, setEmail] = useState('')
  const [senha, setSenha] = useState('')
  const [erro, setErro] = useState<string | null>(null)
  const [loading, setLoading] = useState(false)

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    setLoading(true)
    setErro(null)

    // redirect: false para capturar erro antes de redirecionar
    // (spec usa redirectTo diretamente, mas essa abordagem permite mostrar mensagem de erro)
    const result = await signIn('credentials', {
      email,
      senha,
      redirect: false,
    })

    if (result?.error) {
      setErro('Email ou senha incorretos')
      setLoading(false)
      return
    }

    // router.push não atualiza a sessão do lado do servidor — full reload necessário
    window.location.href = '/kanban'
  }

  return (
    <form onSubmit={handleSubmit} className="flex flex-col gap-4">
      <div className="flex flex-col gap-1">
        <label htmlFor="email" className="text-sm font-medium text-slate-700">
          Email
        </label>
        <input
          id="email"
          type="email"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          required
          autoComplete="email"
          className="border border-slate-300 rounded px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
        />
      </div>
      <div className="flex flex-col gap-1">
        <label htmlFor="senha" className="text-sm font-medium text-slate-700">
          Senha
        </label>
        <input
          id="senha"
          type="password"
          value={senha}
          onChange={(e) => setSenha(e.target.value)}
          required
          autoComplete="current-password"
          className="border border-slate-300 rounded px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
        />
      </div>
      {erro && (
        <p className="text-sm text-red-600">{erro}</p>
      )}
      <button
        type="submit"
        disabled={loading}
        className="bg-blue-600 text-white rounded px-4 py-2 text-sm font-medium hover:bg-blue-700 disabled:opacity-50 disabled:cursor-not-allowed"
      >
        {loading ? 'Entrando…' : 'Entrar'}
      </button>
    </form>
  )
}
```

- [ ] **Step 2: Criar `app/login/page.tsx` (Server Component)**

```tsx
// app/login/page.tsx
import { auth } from '@/auth'
import { redirect } from 'next/navigation'
import { LoginForm } from './LoginForm'

export default async function LoginPage() {
  const session = await auth()
  if (session) redirect('/kanban')

  return (
    <div className="min-h-screen bg-slate-900 flex items-center justify-center p-4">
      <div className="w-full max-w-sm">
        <div className="text-center mb-8">
          <h1 className="text-2xl font-bold text-white">KanbanLicita</h1>
        </div>
        <div className="bg-white rounded-lg shadow-lg p-6">
          <h2 className="text-lg font-semibold text-slate-800 mb-4">Entrar</h2>
          <LoginForm />
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Verificar compilação TypeScript**

```bash
npx tsc --noEmit
```

Esperado: sem erros.

- [ ] **Step 4: Commit**

```bash
git add app/login/
git commit -m "feat(c2a): add login page UI"
```

---

## Task 7: Atualizar rota raiz + script de seed

**Files:**
- Modify: `app/page.tsx`
- Create: `scripts/seed-admin.ts`

- [ ] **Step 1: Atualizar `app/page.tsx`**

Arquivo atual (`app/page.tsx`):
```ts
import { redirect } from 'next/navigation'

export default function Home() {
  redirect('/kanban')
}
```

Substituir por:
```ts
// app/page.tsx
import { auth } from '@/auth'
import { redirect } from 'next/navigation'

export default async function RootPage() {
  const session = await auth()
  if (!session) redirect('/login')
  redirect('/kanban')
}
```

- [ ] **Step 2: Criar `scripts/seed-admin.ts`**

```ts
// scripts/seed-admin.ts
import { db } from '@/lib/db'
import bcrypt from 'bcryptjs'

function arg(name: string): string {
  const idx = process.argv.indexOf(`--${name}`)
  if (idx === -1 || !process.argv[idx + 1]) {
    console.error(`Argumento --${name} é obrigatório`)
    process.exit(1)
  }
  return process.argv[idx + 1]
}

async function main() {
  const email = arg('email').toLowerCase().trim()
  const nome = arg('nome')
  const senha = arg('senha')

  const existente = await db.user.findUnique({ where: { email } })
  if (existente) {
    console.log(`Usuário ${email} já existe. Nenhuma ação necessária.`)
    return
  }

  const hash = await bcrypt.hash(senha, 12)
  await db.user.create({
    data: {
      email,
      name: nome,
      senha: hash,
      role: 'admin',
      ativo: true,
    },
  })

  console.log(`Admin criado com sucesso: ${email}`)
}

main()
  .catch((err) => {
    console.error(err)
    process.exit(1)
  })
  .finally(() => db.$disconnect())
```

- [ ] **Step 3: Verificar compilação TypeScript**

```bash
npx tsc --noEmit
```

Esperado: sem erros.

- [ ] **Step 4: Commit**

```bash
git add app/page.tsx scripts/seed-admin.ts
git commit -m "feat(c2a): update root redirect, add seed-admin script"
```

---

## Task 8: Smoke test de integração

**Files:** nenhum novo arquivo — verificação manual

- [ ] **Step 1: Rodar todos os testes unitários**

```bash
NODE_OPTIONS='--experimental-vm-modules' npx jest --no-coverage
```

Esperado: todos os testes passando (incluindo os 5 novos de `authorize`).

- [ ] **Step 2: Criar o primeiro admin via seed**

```bash
npx tsx scripts/seed-admin.ts \
  --email admin@teste.com \
  --nome "Admin Teste" \
  --senha "senha123"
```

Esperado: `Admin criado com sucesso: admin@teste.com`

Rodar de novo para testar idempotência:
```bash
npx tsx scripts/seed-admin.ts \
  --email admin@teste.com \
  --nome "Admin Teste" \
  --senha "senha123"
```

Esperado: `Usuário admin@teste.com já existe. Nenhuma ação necessária.`

- [ ] **Step 3: Iniciar servidor de desenvolvimento**

```bash
npm run dev
```

- [ ] **Step 4: Testar os cenários manualmente no browser**

| Cenário | URL | Esperado |
|---|---|---|
| Sem sessão, acessa `/` | http://localhost:3000 | Redirect para /login |
| Sem sessão, acessa `/kanban` | http://localhost:3000/kanban | Redirect para /login |
| Sem sessão, acessa `/licitacoes` | http://localhost:3000/licitacoes | Redirect para /login |
| Credenciais erradas | /login | Mensagem "Email ou senha incorretos" |
| Credenciais corretas | /login | Redirect para /kanban |
| Logado, acessa `/login` | /login | Redirect para /kanban |
| Logado, acessa `/` | / | Redirect para /kanban |

- [ ] **Step 5: Commit final se tudo passar**

```bash
git add app/page.tsx app/login/ proxy.ts auth.ts scripts/ types/ prisma/ lib/auth/ __tests__/auth/
git commit -m "feat(c2a): auth complete — login, proxy, session, seed"
```
